{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {% for flashType, flashMessages in app.flashes %}
            {% for flashMessage in flashMessages %}
                {% if flashType == 'notice' %}
                    <br>
                    <div class="alert alert-success">
                        {{ flashMessage }}
                    </div>
                {% elseif flashType == 'error' %}
                    <br>
                    <div class="alert alert-danger">
                        {{ flashMessage }}
                    </div>
                {% endif %}
            {% endfor %}
        {% endfor %}

        <table class="table table-striped">
            <thead class="thead-dark">
                <tr>
                    <th>Name</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for job in jobs %}
                    <tr>
                        <td>{{ job.name }}</td>
                        <td>
                            <a href="{{ path('delete_job', {'id': job.id}) }}" class="btn btn-danger">Delete</a>
                        </td>
                    </tr>
{% endfor %}
            </tbody>
        </table>
        <a href="{{ path('create_jobs') }}" class="btn btn-success  mb-3">Create</a>
    </div>
{% endblock %}
